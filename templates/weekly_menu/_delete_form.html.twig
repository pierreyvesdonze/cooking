<form method="post" action="{{ path('app_weekly_menu_delete', {'id': weekly_menu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ weekly_menu.id) }}">
    <button class="btn">Delete</button>
</form>
